.venv/
__pycache__/
*.pyc
.env
.ipynb_checkpoints/
models/*.joblib
node_modules/
dist/

# data/ is deliberately NOT ignored: it is only ~1.2 MB and committing it lets a judge
# reproduce every reported number without running the generator.
